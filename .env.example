# —— CLI 指定（可写命令名或绝对路径；留空则自动探测）——
AI_CLI=/Users/test/.claude/local/claude
# 备选后端（空格分隔；留空就用默认顺序）
AI_BACKENDS="claude"

# —— 前缀/后缀规则 —— 
TASK_INCLUDE_PREFIX="++"           # 参与处理
# 这三个是“忽略目录”的前缀（固定即可，不建议改）
# 目录名以 -, !, __ 开头（以及 archive/）会被整目录跳过
ARCHIVE_DIR="archive"

STATUS_SUFFIX_DOING=".doing.md"
STATUS_SUFFIX_DONE=".done.md"
STATUS_SUFFIX_BLOCKED=".blocked.md"

# —— 输出/日志目录（相对 tasks/）——
OUTPUTS_SUBDIR="outputs"
LOGS_SUBDIR="logs"